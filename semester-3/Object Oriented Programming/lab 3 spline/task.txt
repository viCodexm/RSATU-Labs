
task 3. Сплайны
TDat массив точек (x, y)
    GetY(int x)

LineSpline y=kx+b
    GetY(int x)

TKSpline кубический сплайн
    y=ax^3+bx^2+cx+d

    (крамер/гаусс или как угодно)

Создаем три объекта разных типов
конструктор должен заполнять массив точек
запрашиваем у пользователя координату x и по ней 
вызываем GetY для каждого из объектов
выводим что получилось

нужно выбрать как минимум 4 точки чтобы хоть что то было
