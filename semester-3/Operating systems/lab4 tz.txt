1.CreateProcess
(чтобы тонко управлять процессом, пытается открыть исполняемый образ)
в первой строчке путь к запускаемому файлу
NULL
NULL
FALSE
0 или нормальный процесс
NULL
NULL / current directory -*текущая директория для процесса
GetStartupInfo берем и копируем структуру
указатель на специальную структуру, куда будет возвращена операция

id не интересуют, а handle (локальный индентификатор, по которому можно обратиться к процессу) интересуют

PROCESS_INFORMATION pi; передаем CreateProcess'у
WaitForSingleObject(pi.hProcess, INFINITE)

хэндл - отмеченно(завершился)/не отмеченно(работает)
обязательно закрыть хэндлы CloseHandle
иначе он будет висеть пока наш процесс не завершится
CloseHandle(pi.hProcess)
CloseHandle(pi.hThread)
посмотреть коды возврата на всякий случай


2.WinExec - тоже запустить калькулятор

3.Открыть ассоциативный файл
(открыть документ/таблицу через word/excel)
ShellExecute

STARTAPINFO
cb
STARTF_USESHOWWINDOW
SW_SHOW