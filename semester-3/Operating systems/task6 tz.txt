1. Передать два процесса.
один процесс передатчик, второй процесс приемник
читаем строку 		отображаем строку

1 способ:
SendMessage, WM_COPYDATA можно передать окну
			создает окно (взять шаблон оконного приложения)
			чтобы был обработчик сообщений. задать уникальный класс.
FindWindow(имя класса, ..) - получим hwnd
SendMessage(..WM_COPYDATA)


3 способ:
через сокеты
TCP		connect, send, recv
bind, accept ..


2 способ:
mapping
memorymappedfile
hMap = CreateFileMappingA(последний парметр - имя (свое уникальное)) есть lpName - можем получить доступ из разных процессов

вместо hFile можно задать INVALID_HANDLE_VALUE - создаст объект без привязки к файлу

dwMaximumSizeHigh(Low) - размеры окошечка

размер мэппинга всегда кратен 4м кб
pMem = MapViewOfFile -указатель на начало памяти

во втором процессе делаем то же самое и два процесса получают общую память

теперь нужно узнавать что там появляется что-то полезное - потребуется объект синхронизации Event

в кажом процессе создаем ивент
hEvent = CreateEvent(NULL, false, 

строку можно записать в начало pMem. в конце добавить #0

смотрим чтобы не возникали ошибки
если что есть GetLastError

передатчик копирует строку и делает SetEvent

приемник WaitForSingleObject и печатает строку



